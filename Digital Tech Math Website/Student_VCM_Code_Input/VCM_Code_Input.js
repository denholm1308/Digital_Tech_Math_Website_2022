//START OF JS CODE --->

function generate_learning_plan(){
    const codes = new Set(["a","b","c"]);
   // var codes = ["lol", "poop", "denholm"]
    var vcm_code = document.getElementById("VCM_textbox")
    if (vcm_code.value = codes[1]){
        alert("lol");
    } else {
        alert("YES BUDDY");
    }
}

